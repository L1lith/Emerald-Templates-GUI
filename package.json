{
  "name": "emt-gui",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "cd gui && astro dev",
    "start": "npm run build && neu run",
    "build": "cd gui && astro build",
    "preview": "cd gui && astro preview"
  },
  "devDependencies": {
    "@astrojs/solid-js": "^0.1.2",
    "astro": "^1.0.0-beta.36",
    "sass": "^1.52.1",
    "solid-js": "^1.4.3"
  }
}
